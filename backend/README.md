# Backend (FastAPI)

This folder will contain the FastAPI-based API gateway for job orchestration, authentication, and communication with ComfyUI's API/WebSocket. It will also handle signed R2 uploads and proxy privileged operations for the frontend.
