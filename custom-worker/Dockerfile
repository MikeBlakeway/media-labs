# Start from the official worker-comfyui base image
FROM runpod/worker-comfyui:5.4.1-base

# The base image already has S3 upload capability built-in!
# We just need to configure environment variables for Backblaze B2
# No additional dependencies needed.