# MMI-005: FLUX.1 Text-to-Image Handler

## Details

### Problem

The multi-modal inference worker needs to implement text-to-image generation using FLUX.1 Schnell fp8 model as the primary image generation capability. This is the most fundamental modality and serves as the foundation for other image-based modalities like ControlNet and inpainting.

### Impact

Without FLUX.1 text-to-image implementation:

- Core image generation functionality will be unavailable
- Users cannot generate images from text prompts
- Foundation for other image modalities (ControlNet, inpainting) will be missing
- The system cannot demonstrate basic AI inference capabilities
- Performance benchmarking and optimization cannot begin

### Scope

This story covers implementing the complete FLUX.1 Schnell handler including model loading, inference pipeline, parameter validation, and output formatting. Requires MMI-003 (Model Management) and MMI-004 (Handler Routing) to be completed first.

---

## Acceptance criteria

- Given a text prompt, when text-to-image inference is requested, then FLUX.1 generates a high-quality image within 15 seconds.
- Given image parameters (width, height, steps), when provided, then the generated image matches the specified dimensions and quality settings.
- Given invalid parameters, when validation fails, then clear error messages are returned explaining the parameter constraints.
- Given memory constraints, when FLUX.1 is loaded, then it integrates with the model management system for efficient memory usage.
- Given concurrent requests, when multiple text-to-image jobs run, then each completes successfully without interference.
- All generated images must be properly encoded and returned in the standardized response format.

---

## Checklist

### Inspect and modify

- [ ] `workers/multi-model-worker/src/handlers/flux_handler.py` — implement FluxHandler class with FLUX.1 Schnell fp8 integration.
- [ ] `workers/multi-model-worker/src/models/flux_model.py` — create FLUX.1 model wrapper with optimized loading and inference.
- [ ] `workers/multi-model-worker/src/utils/image_utils.py` — implement image encoding, validation, and format conversion utilities.
- [ ] `workers/multi-model-worker/src/schemas/text_to_image_schema.py` — define request/response schemas for text-to-image operations.
- [ ] `workers/multi-model-worker/scripts/download_flux_model.py` — script to download and validate FLUX.1 Schnell fp8 model.
- [ ] `workers/multi-model-worker/src/handlers/multi_modal_handler.py` — register FLUX.1 handler in the routing system.

### Tests

- [ ] Unit: `tests/unit/test_flux_handler.py` — comprehensive tests for FLUX.1 handler including parameter validation and error handling.
- [ ] Unit: `tests/unit/test_flux_model.py` — tests for FLUX.1 model loading, inference, and memory management integration.
- [ ] Integration: `tests/integration/test_text_to_image_workflow.py` — end-to-end tests for complete text-to-image pipeline.
- [ ] Performance: `tests/performance/test_flux_benchmarks.py` — performance tests ensuring <15 second inference times.

### General

- [ ] Use FLUX.1 Schnell fp8 quantized version for optimal memory usage (12-15GB target).
- [ ] Implement proper guidance scale handling (Schnell uses 0.0 guidance).
- [ ] Include comprehensive parameter validation with sensible defaults.
- [ ] Integrate with model management system for efficient loading and eviction.
- [ ] Follow established image encoding patterns from existing Media Labs workers.
- [ ] Documentation includes FLUX.1 capabilities, parameters, and performance characteristics.

---

## Definition of Done

- [ ] All acceptance criteria satisfied with functional FLUX.1 text-to-image generation.
- [ ] FLUX.1 Schnell fp8 model loads successfully and generates high-quality images.
- [ ] Inference times consistently meet <15 second target for standard 1024x1024 images.
- [ ] Parameter validation handles all edge cases with clear error messages.
- [ ] Model integrates seamlessly with memory management system.
- [ ] Comprehensive test suite covers all text-to-image scenarios and performance requirements.
