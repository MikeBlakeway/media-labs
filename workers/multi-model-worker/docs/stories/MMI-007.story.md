# MMI-007: AnimateDiff Image-to-Video

## Details

### Problem

The multi-modal inference worker needs image-to-video capabilities using AnimateDiff to transform static images into short video sequences with natural motion. This expands the system's capabilities beyond static image generation into dynamic video content creation.

### Impact

Without AnimateDiff image-to-video functionality:

- Users cannot create video content from existing images
- The system lacks motion generation capabilities
- Video workflows cannot be established as foundation for other video modalities
- Competitive advantage in multi-modal content creation is reduced
- Integration with existing Media Labs video templates will be impossible

### Scope

This story covers implementing the AnimateDiff handler for image-to-video generation, including motion adapter integration, frame sequence processing, and video encoding capabilities as specified in the strategy document.

---

## Acceptance criteria

- Given an input image and motion prompt, when image-to-video inference is requested, then AnimateDiff generates smooth 16-frame video within 25 seconds.
- Given video parameters (frames, fps), when specified, then the generated video matches the requested duration and frame rate settings.
- Given various input image formats, when processed, then the system handles different aspect ratios and resolutions appropriately.
- Given memory constraints, when AnimateDiff loads, then it integrates with model management for efficient resource usage.
- Given motion prompts, when provided, then the generated motion reflects the textual guidance while maintaining image consistency.
- All generated videos must be properly encoded and returned in standardized format compatible with Media Labs video display components.

---

## Checklist

### Inspect and modify

- [ ] `workers/multi-model-worker/src/handlers/animatediff_handler.py` — implement AnimateDiffHandler with motion adapter integration.
- [ ] `workers/multi-model-worker/src/models/animatediff_model.py` — create AnimateDiff model wrapper with motion processing capabilities.
- [ ] `workers/multi-model-worker/src/utils/video_utils.py` — implement video encoding, frame processing, and format conversion utilities.
- [ ] `workers/multi-model-worker/src/schemas/image_to_video_schema.py` — define request/response schemas for image-to-video operations.
- [ ] `workers/multi-model-worker/scripts/download_animatediff_models.py` — script to download AnimateDiff motion adapter model.
- [ ] `workers/multi-model-worker/src/handlers/multi_modal_handler.py` — register AnimateDiff handler in routing system.

### Tests

- [ ] Unit: `tests/unit/test_animatediff_handler.py` — tests for AnimateDiff inference, motion processing, and parameter validation.
- [ ] Unit: `tests/unit/test_video_utils.py` — tests for video encoding, frame processing, and format validation.
- [ ] Integration: `tests/integration/test_image_to_video_workflow.py` — end-to-end tests for complete image-to-video pipeline.
- [ ] Performance: `tests/performance/test_animatediff_benchmarks.py` — performance tests ensuring <25 second inference times.

### General

- [ ] Use AnimateDiff motion adapter as specified (2GB target size) with efficient loading.
- [ ] Implement proper frame interpolation and motion synthesis for smooth video output.
- [ ] Support configurable video parameters (frames, fps, duration) with sensible defaults.
- [ ] Integrate with model management system for memory-efficient operation alongside other modalities.
- [ ] Follow established video encoding patterns compatible with Media Labs video display components.
- [ ] Documentation includes AnimateDiff capabilities, motion parameters, and video format specifications.

---

## Definition of Done

- [ ] All acceptance criteria satisfied with functional AnimateDiff image-to-video generation.
- [ ] AnimateDiff motion adapter generates smooth, temporally consistent 16-frame videos.
- [ ] Inference times consistently meet <25 second target for standard image-to-video conversion.
- [ ] Video encoding produces compatible formats that integrate with existing Media Labs video components.
- [ ] Model integrates seamlessly with memory management system for concurrent operation.
- [ ] Comprehensive test suite covers all image-to-video scenarios and performance requirements.
